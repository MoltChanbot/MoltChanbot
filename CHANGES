There is no existing code. Please write it as a new file.

- In app/page.tsx, inside HeaderInner()’s nav:
  1) Change link labels to feel different (anchors remain the same):
     - Replace:
       <Link href="#why" className="px-2 hover:text-white">Why Nexro</Link>
       with:
       <Link href="#why" className="px-2 hover:text-white">Edge</Link>

     - Replace:
       <Link href="#how" className="px-2 hover:text-white">How it Works</Link>
       with:
       <Link href="#how" className="px-2 hover:text-white">Pipeline</Link>

     - Replace:
       <Link href="#playbooks" className="px-2 hover:text-white">Playbooks</Link>
       with:
       <Link href="#playbooks" className="px-2 hover:text-white">Strategies</Link>

     - Keep Terminal and Markets as-is.

  2) Immediately after the <SolanaPriceTicker /> and before the X link, insert a compact CTA button:
     {`<Button
        asChild
        className="ml-1 hidden h-8 px-3 text-xs text-white ring-1 ring-white/20 hover:brightness-110 sm:inline-flex"
        style={{ background: "linear-gradient(135deg, rgba(124,58,237,1) 0%, rgba(139,92,246,1) 45%, rgba(99,102,241,1) 100%)" }}
      >
        <Link href="#chat">Get Started</Link>
      </Button>`}

- In app/page.tsx, in the HERO content:
  1) Replace the existing <h1>...</h1> with this updated, different title (distinct wording and emphasis):
     {`<h1 className="mx-auto max-w-5xl text-balance text-3xl font-bold leading-tight tracking-tight sm:text-5xl md:text-6xl">
        Sonic Perps for{" "}
        <NeonText as="span" className="font-bold" color="#C4B5FD">
          Autonomous Agents
        </NeonText>{" "}
        on{" "}
        <NeonText as="span" className="font-bold" color="#7C3AED">
          Solana
        </NeonText>
      </h1>`}

  2) Replace the paragraph under the title with a refreshed narrative (short, distinct, wave/sonic-themed):
     {`<p className="mx-auto mt-6 max-w-2xl text-pretty text-base text-slate-200 md:text-lg">
        Nexro Dex is a wave‑driven execution layer: agents sense the orderbook, simulate paths, and fill with MEV‑aware routing, cross‑margin safeguards, and real‑time telemetry. Built for speed, safety, and composability.
      </p>`}

  3) Replace the Badge text for a different hook:
     - Replace:
       Future-native Perp Automation
       with:
       Wave‑Driven Execution

- In app/docs/page.tsx:
  1) Change the H1 to a new title:
     - Replace:
       <h1 id="overview">Nexro Dex</h1>
       with:
       <h1 id="overview">Nexro Dex — Sonic Perps for Agents</h1>

  2) Replace the first paragraph under Overview with a new narrative:
     {`<p>
        Nexro Dex is a sonic‑fast Perpetual DEX and AI Agent Framework on Solana. Agents observe markets, plan with policy modules, and execute via a router tuned for realized execution (slippage, fees, funding) while enforcing strict guardrails. This site is a static, frontend‑only demo.
      </p>`}

  3) In Quick Start step 1, make it reference the Sonic Terminal as a distinct callout:
     - Replace the first list item content with:
       Open the homepage and try the Sonic Terminal or Agent Demo — responses are local placeholders.

- In components/chat-box.tsx:
  1) Update the initial assistant message to match the new tone:
     - Replace the initial state with:
       {`[
          {
            role: "assistant",
            content:
              "Welcome to Nexro Dex — sonic perps for autonomous agents. Ask about routing, risk controls, or the DSL. This demo is fully client‑side.",
          },
        ]`}
